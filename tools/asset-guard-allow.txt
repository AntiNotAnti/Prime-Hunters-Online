# Paths the asset guard may ignore, one glob per line.
#
# Only for files that are ours: a logo, a screenshot of the launcher, a
# diagram. Never a map preview, a model, a sound, or anything else that came
# out of the cartridge -- those are produced on each player's own machine and
# do not belong in this repository or in a release.

# Project Prime's renderer-owned visual marks. The standard mark is embedded
# for the game window and is also the source used by macOS packaging; the
# server variant adds the dedicated-server badge.
src/MphRead/Assets/project-prime-mark.png
src/MphRead/Assets/project-prime-mark-server.png
src/MphRead/Assets/project-prime-banner.png

# The launcher's legacy backdrop fixture. The player-facing modern hub now uses
# locally generated map art and graded fallbacks, but this exact file remains
# available to compatibility/design checks. It is exempted by exact path so a
# second game-derived picture cannot slip through the guard accidentally.
src/MphRead/Assets/Backgrounds/launcher-bg.png
